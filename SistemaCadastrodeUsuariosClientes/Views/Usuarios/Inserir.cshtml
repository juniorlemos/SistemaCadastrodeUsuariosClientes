@model SistemaCadastrodeUsuariosClientes.Models.Usuario

<h2>Inserir</h2>





@*<form id="friendform">
        <table>
            <tr>
                <td>Friend Name</td>
                <td><input id="nomeCompleto" name="NomeCompleto" type="text" /></td>
            </tr>
            <tr>
                <td>Phone</td>
                <td><input id="apelido" name="Apelido" type="text" /></td>
            </tr>
            <tr>
                <td>State</td>
                <td><input id="email" name="Email" type="text" /></td>
            </tr>
            <tr>
                <td>State</td>
                <td><input id="telefone" name="TelefoneDeContato" type="text" /></td>
            </tr>
        </table>
        <input id="btnsubmit" type="button" value="Submit" />
    </form>*@




<form id="formularioUsuario">

    <div class="form-group ">
        <label for="nomeCompleto">Nome Completo</label>
        <input type="text"  class="form-control form-control-border border-width-2" id="nomeCompleto" placeholder="Digite seu Nome Completo">
    </div>
    <div class="form-group">
        <label for="apelido">Apelido</label>
        <input type="text"  class="form-control form-control-border border-width-2" id="apelido" placeholder="Digite seu Apelido ">
    </div>

    <div class="form-group">
        <label for="email">Email</label>
        <input type="email"  class="form-control form-control-border border-width-2" id="email" placeholder="Digite seu Email">
    </div>
    <div class="form-group">
        <label for="telefone">Telefone</label>
        <input type="tel"  class="form-control form-control-border border-width-2" id="telefone" pattern="\([0-9]{2}\)[0-9]{4,6}-[0-9]{3,4}$" placeholder="digite seu telefone">
    </div>

    <button id="btnsubmit" type="submit" class="btn btn-primary">Salvar</button>
</form>







@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}


    <script>

        $(document).ready(function () {
            $("#btnsubmit").click(function (e) {

                var valdata = $("#friendform").serialize();

                $.ajax({
                    url: "/Usuarios/Inserir",
                    type: "POST",
                    data: valdata,
                    success: function () {

                        $('#friendform').each(function () {
                            this.reset();
                        });

                    },

                });



            });
        });
    </script>



    <script>

        $(document).ready(function () {
            $("#btnsubmi").click(function (e) {

                var valdata = $("#formularioUsuario").serialize();

                alert(valdata);

            });
        });
    </script>
}